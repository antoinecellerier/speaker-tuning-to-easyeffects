name: Claude Code

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned]
  pull_request_review:
    types: [submitted]

jobs:
  claude:
    if: |
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review' && contains(github.event.review.body, '@claude')) ||
      (github.event_name == 'issues' && (contains(github.event.issue.body, '@claude') || contains(github.event.issue.title, '@claude')))
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
      issues: read
      id-token: write
      actions: read # Required for Claude to read CI results on PRs
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      # Grant read-only gh access to exactly the issues/PRs the trigger text
      # references (#N), so "@claude … as suggested in #33" can read #33 but
      # nothing else. Trigger text is safe to scan: the action only runs for
      # write-access actors. Passed via env, never inlined into the script —
      # inlining ${{ …body }} in run: is a shell-injection hole.
      - name: Allowlist gh reads for issues referenced in the trigger
        id: refs
        env:
          TRIGGER_TEXT: >-
            ${{ github.event.comment.body
             || github.event.review.body
             || github.event.issue.body }}
        run: |
          tools=""
          for n in $(grep -oE '#[0-9]+' <<< "$TRIGGER_TEXT" | tr -d '#' | sort -un); do
            tools="${tools:+$tools,}Bash(gh issue view $n:*),Bash(gh pr view $n:*),Bash(gh pr diff $n:*)"
          done
          echo "allowed_tools=$tools" >> "$GITHUB_OUTPUT"

      - name: Run Claude Code
        id: claude
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}

          # This is an optional setting that allows Claude to read CI results on PRs
          additional_permissions: |
            actions: read

          # Optional: Give a custom prompt to Claude. If this is not specified, Claude will perform the instructions specified in the comment that tagged it.
          # prompt: 'Update the pull request description to include a summary of changes.'

          # See https://github.com/anthropics/claude-code-action/blob/main/docs/usage.md
          # or https://code.claude.com/docs/en/cli-reference for available options
          claude_args: ${{ steps.refs.outputs.allowed_tools != '' && format('--allowedTools "{0}"', steps.refs.outputs.allowed_tools) || '' }}

