# Screenshots

The main `README.md` and the user guides in `docs/` reference these
EasyEffects GUI screenshots. Keep these exact filenames so those references
resolve.

| File | Shows | Used by | Status |
|------|-------|---------|--------|
| `ee-preset-select.jpg` | Presets panel listing the generated `Dolby-*` presets | README Quick start | ✅ captured |
| `ee-version.jpg` | About dialog showing the EasyEffects **8.x** version | troubleshooting.md | ✅ captured |
| `ee-chain-loaded.jpg` | Effects chain Convolver → Limiter, with Autogain bypassed | README How it works | ✅ captured |
| `ee-autoload.jpg` | Autoload dialog: a `Dolby-*` preset bound to the speaker, `Nothing` as the global fallback | dolby-to-easyeffects.md Autoload | ✅ captured |
| `ee-global-bypass.jpg` | The global on/off toggle at the top left, next to "Effects", annotated with a red highlight | troubleshooting.md | ✅ captured |
| `ee-background-service.jpg` | Background Service prefs, with the *Enable service mode* and *Autostart on login* toggles both on | troubleshooting.md | ✅ captured |

Crop to the relevant panel and use the default theme. Keep widths to a
reasonable ~900 px so they render cleanly inline on GitHub.
