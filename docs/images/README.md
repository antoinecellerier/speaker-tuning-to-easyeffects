# Screenshots

The main `README.md` references these EasyEffects GUI screenshots. Keep these
exact filenames so the README references resolve.

| File | Shows | Used by README section | Status |
|------|-------|------------------------|--------|
| `ee-preset-select.jpg` | Presets panel listing the generated `Dolby-*` presets | Quick start | ✅ captured |
| `ee-version.jpg` | About dialog showing the EasyEffects **8.x** version | Troubleshooting | ✅ captured |
| `ee-chain-loaded.jpg` | Effects chain Convolver → Limiter, with Autogain bypassed | How it works | ✅ captured |
| `ee-autoload.jpg` | Autoload dialog: a `Dolby-*` preset bound to the speaker, `Nothing` as the global fallback | Autoload | ✅ captured |
| `ee-global-bypass.jpg` | The global on/off toggle at the top left, next to "Effects", annotated with a red highlight | Troubleshooting | ✅ captured |
| `ee-background-service.jpg` | Background Service prefs, with the *Enable service mode* and *Autostart on login* toggles both on | Troubleshooting | ✅ captured |

Crop to the relevant panel and use the default theme. Keep widths to a
reasonable ~900 px so they render cleanly inline on GitHub.
